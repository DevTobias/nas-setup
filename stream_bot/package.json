{
  "name": "stream_bot",
  "private": true,
  "license": "MIT",
  "scripts": {
    "start": "pnpm -r --filter discord run",
    "upgrade": "pnpm -r upgrade --latest",
    "clean": "pnpm dlx rimraf **/dist **/node_modules"
  }
}
